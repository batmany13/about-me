{
  "$comment": "Copy to <repo>/.claude/catchup.config.json and delete what you don't need. Every field is optional — the skill works with no config at all.",

  "repo": {
    "label": "my-repo"
  },

  "output": {
    "dir": ".claude/catchups"
  },

  "authors": {
    "$comment": "Turn split on only for a repo with more than one real contributor. Every commit email must be listed, or it lands in unknown_authors.",
    "split": false,
    "people": [
      { "name": "First Last", "emails": ["someone@example.com", "someone@work.example"] }
    ]
  },

  "categories": {
    "$comment": "paths/keywords EXTEND the built-in rules. Use paths_replace/keywords_replace to discard them instead. Titles are yours to change; the three keys are not.",
    "meeting": {
      "title": "Meeting / Partner Notes",
      "paths": ["notes/calls/**"],
      "keywords": ["board", "advisory"]
    },
    "technical": {
      "title": "Technical Notes",
      "paths": ["engine/**"],
      "keywords": ["pipeline"]
    },
    "other": {
      "title": "Other"
    }
  },

  "summary": {
    "$comment": "Raise these for a repo that routinely runs hundreds of commits a week.",
    "bullets": "6-12",
    "words": "150-250"
  },

  "ignore": {
    "$comment": "Bookkeeping commits — excluded from counts but always reported as a number. These EXTEND the built-ins (.claude/transcripts, lockfiles, chore(transcripts): ...).",
    "paths": [],
    "subjects": []
  },

  "deepvista": {
    "$comment": "Sync entities to DeepVista context cards over the hosted MCP server. Off by default.",
    "enabled": false,
    "project_id": null,
    "tags": ["catchup"],
    "card_status": "confirmed",
    "card_types": {
      "$comment": "Override the entity-type -> DeepVista card_type mapping. Values must be DeepVista's own card types.",
      "thread": "topic"
    }
  },

  "git": {
    "$comment": "Off by default — catchups are working artifacts and landing them is the user's call.",
    "commit": false,
    "pr": false
  }
}
